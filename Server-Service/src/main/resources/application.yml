server:
  port: 8082

spring:
  application:
    name: server-service
  datasource:
    url: jdbc:postgresql://localhost:5432/server_db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect